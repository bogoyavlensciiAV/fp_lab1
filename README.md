# lab1 on gleam⭐(Богоявленский Александр P3317)
## Вариант 8, 22
## Описание проблемы
Задание 8: Найти в n-значном числе максимальное произведение l-подряд идущих цифр (в задаче n=1000, l=13)

Задание 22: Найти сумму счетов* всех имён в файле

*Счёт имени определяется по принципу: (сумма позиций букв имени в алфавите) * (Позиция имени в отсортированном списке имён в файле)

## Ключевые элементы реализации
Для каждой задачи представлено несколько реализаций:

- рекурсивная (обычная и хвостовая),

- модульная реализация с использованием (filter, fold, range),

- с генерацией последовательности при помощи отображения (map)

Применены функции из стандартной библиотеки Gleam (list, string, int, result) + библиотека для чтения файлов (simplifile).

Логика разбита на небольшие чистые функции (проверка входа, генерация окон, вычисление произведения, поиск максимума и тд).

Обработка ошибок через тип Result(_, String) — некорректные символы, нулевая или отрицательная длина окна, строка короче окна — сразу возвращают Error.

Использованы пайплайны (|>), деструктуризация списка ([name, ..rest]), use-expression (use x <- result.try(function(x))), let assert
## Вывод
Пока пишешь на глиме, ощущаешь что язык современный (почти все штуки для работы с проектом есть из коробки, тесты изначально подлючены и тд). После того как поделал пару пайплайнов, очень проникся ими, хотя с непривычки от Result было очень стрёмно. Но вместе с тем не было ни разу чтобы программа свалилась во время выполнения, чувствуется что авторы очень уж стремились сделать язык безопасным

Способ подключения библиотек и своих модулей мне по душе, приятный

Работа со строками местами удивляет (например что нет метода для получения символа по индексу)

Use-expression по заявлениям должен сделать код понятнее, хотя кроме случая с result.try вообще не понятнее чё происходит (при первом просмотре)

А в целом прикольный язык, хотя пока я не могу себе однозначно ответить для чего он нужен (кроме того что автор хотел строгую типизацию в ерланге)

